-- pairing up the structures so they "fit" together
g = (0,0)
c = (1,1)
t = (0,1)
a = (1,0)

and :: (Int,Int) -> (Int,Int) -> (Int,Int)
or :: (Int,Int) -> (Int,Int) -> (Int,Int)
not :: (Int,Int) -> (Int,Int)
